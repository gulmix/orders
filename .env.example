# Скопируйте в .env и правьте локально: cp .env.example .env
# .env подхватывает make run; сам сервис читает только переменные окружения.

ORDERS_HTTP_ADDR=:8080

ORDERS_SHUTDOWN_TIMEOUT=10s

ORDERS_LOG_LEVEL=info
